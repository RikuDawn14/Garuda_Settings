set number " Add line numbers 
syntax on " Color syntax highlighting
cmap w!! w !sudo tee > /dev/null % " Sudo within Vim
set list
set listchars=tab:>- " Show tabs as characters
set tabstop=4 " Tab distance
